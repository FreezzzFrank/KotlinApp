<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="contributor"
            type="com.example.kotlinapp.sample.githubbrowser.repository.db.entities.Contributor" />
    </data>

    <com.example.kotlinapp.views.MaskedCardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:cardUseCompatPadding="true">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <ImageView
                android:id="@+id/imageView"
                android:layout_width="@dimen/photo_size"
                android:layout_height="@dimen/photo_size"
                android:layout_marginVertical="@dimen/margin_small"
                android:layout_marginStart="@dimen/margin_small"
                android:adjustViewBounds="false"
                android:contentDescription="@string/repo_contributor_content_description"
                android:cropToPadding="false"
                android:scaleType="centerCrop"
                app:transitionName="@{contributor.login}"
                app:imageFromUrl="@{contributor.avatarUrl}"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                app:srcCompat="@android:drawable/star_off" />

            <TextView
                android:id="@+id/textView"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="@dimen/margin_small"
                android:layout_marginTop="@dimen/margin_small"
                android:text="@{contributor.login}"
                app:layout_constraintStart_toEndOf="@+id/imageView"
                app:layout_constraintTop_toTopOf="parent"
                tools:text="Layne Yiu" />

        </androidx.constraintlayout.widget.ConstraintLayout>

    </com.example.kotlinapp.views.MaskedCardView>


</layout>
